# myllm — 从零手搓大语言模型

从最简单的 Bigram 模型开始，逐步构建一个完整的 Transformer 语言模型。用《三国演义》作为训练数据，每一步都配有详细的中文教程文章。

## 学习路线

| 步骤 | 内容 | 状态 |
|------|------|:----:|
| 第 1 步 | [Bigram 模型](articles/01-baby-step-bigram-model.md) — 字符级分词 + Embedding + 训练循环 + 文本生成 | ✅ |
| 第 2 步 | Self-Attention — 让模型能"看到"前文多个字符 | 🔜 |
| 第 3 步 | Multi-Head Attention + FFN — 构成完整 Transformer Block | 🔜 |
| 第 4 步 | Mini-GPT — 多层 Transformer 堆叠 | 🔜 |
| 第 5 步 | BPE 分词器 — 从字符级升级到子词级 | 🔜 |
| 第 6 步 | 自定义数据微调 | 🔜 |

## 快速开始

```bash
# 安装依赖
uv sync

# 下载训练数据（三国演义）
mkdir -p data
curl -o data/input.txt https://raw.githubusercontent.com/tennessine/corpus/master/%E4%B8%89%E5%9B%BD%E6%BC%94%E4%B9%89.txt

# 训练模型
uv run python train.py

# 生成文本
uv run python generate.py
uv run python generate.py --prompt "却说曹操" --length 200
```

## 技术栈

- Python 3.10+
- PyTorch
- uv (包管理)

## 项目结构

```
myllm/
├── model.py           # 模型定义
├── train.py           # 训练脚本
├── generate.py        # 文本生成
├── data/input.txt     # 训练数据（需下载，不在 git 中）
├── articles/          # 配套教程文章（中文）
└── docs/              # 项目文档
```

## 配套文章

本项目的每一步都有配套文章，发布在微信公众号上：

1. [从零手搓大语言模型（一）：用三国演义训练你的第一个语言模型](articles/01-baby-step-bigram-model.md)
